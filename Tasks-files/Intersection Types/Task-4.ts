// Problem 4: You have: `type Dog = { bark(): void }; type Animal = { legs: number };`. Make an
// intersection type `PetDog` and write a function that takes `PetDog` and calls `bark()` and logs the
// leg count.
